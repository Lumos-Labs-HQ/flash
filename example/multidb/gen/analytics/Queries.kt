// Code generated by FlashORM. DO NOT EDIT.

package analytics

import java.sql.Connection

/**
 * Unified query interface. Use `Queries.newq(conn)` to obtain an instance.
 */
class Queries(private val conn: Connection) {

    private val events = EventsQueries(conn)

    companion object {
        fun newq(conn: Connection): Queries = Queries(conn)
    }

    fun getRecentEvents(limit: Int): List<Events> =
        events.getRecentEvents(limit)

    fun trackEvent(args: TrackEventParams): Events? =
        events.trackEvent(args)

    fun getMetricsByName(metric: String, limit: Int): List<Metrics> =
        events.getMetricsByName(metric, limit)

    fun close() {
        events.close()
    }
}
